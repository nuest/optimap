[OPTIMAP] 🌍 Region backfill — {{ tally.updated }} work(s) updated

Hello,

Deployment: {{ base_url }}

The scheduled global-region backfill sweep linked works to their continents and
oceans via the offline point-in-polygon join against the GlobalRegion outlines.

Summary:
- Works processed: {{ tally.processed }}
- Works updated (regions linked): {{ tally.updated }}
- … of which span multiple regions: {{ tally.multi_region }}
- No polygon match (retried next run): {{ tally.no_match }}
- Errors: {{ tally.errors }}

Works with no polygon match are retried automatically on the next weekly run.
If "Errors" is non-zero, check the application logs for details.
